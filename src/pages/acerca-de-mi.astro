---
import GlitchLayout from "../layouts/GlitchLayout.astro";
import { UI_CONFIG } from "../constants";
---

<GlitchLayout title={UI_CONFIG.PAGE_TITLES.ABOUT}>
	<div class="about-container">
		<div class="container">
			<!-- Header de la página -->
			<header class="about-header">
				<h1 class="about-title">Acerca de mí</h1>
				<p class="about-subtitle">
					Conoce al gamer detrás de las reseñas y opiniones
				</p>
			</header>

			<!-- Contenido principal -->
			<section class="about-content">
				<div class="about-card">
					<div class="avatar-section">
						<div class="avatar">
						<img src="/img/general/1.png" alt="GlitchMaster Avatar" style="width: 100%; height: 100%; object-fit: cover;">
					</div>
					<h2 class="gamer-tag">GlitchMaster</h2>
						<p class="gamer-subtitle">Crítico Gaming & Desarrollador</p>
					</div>

					<div class="bio-section">
						<h3>Mi Historia Gaming</h3>
						<p>
							¡Hola, gamers! Soy un apasionado de los videojuegos con más de 15 años de experiencia 
							explorando mundos virtuales, desde los clásicos arcade hasta los últimos AAA. 
							Mi amor por los videojuegos comenzó con una vieja consola de 8 bits y desde entonces 
							no he parado de jugar, analizar y compartir mis experiencias.
						</p>

						<p>
							Como desarrollador de software de día y gamer de noche, tengo una perspectiva única 
							sobre la industria. Entiendo tanto la magia que hay detrás del código como la 
							emoción de vivir una gran historia interactiva. Esta combinación me permite 
							ofrecer reseñas técnicamente informadas pero siempre enfocadas en la diversión.
						</p>

						<h3>¿Qué encontrarás aquí?</h3>
						<div class="features-grid">
							<div class="feature-item">
								<span class="feature-icon">🔍</span>
								<h4>Reseñas Honestas</h4>
								<p>Análisis profundos sin spoilers, enfocados en mecánicas, narrativa y diversión.</p>
							</div>
							<div class="feature-item">
								<span class="feature-icon">💭</span>
								<h4>Opiniones Personales</h4>
								<p>Reflexiones sobre tendencias, controversias y el futuro de la industria gaming.</p>
							</div>
							<div class="feature-item">
								<span class="feature-icon">📰</span>
								<h4>Noticias Relevantes</h4>
								<p>Las últimas novedades del mundo gaming que realmente importan.</p>
							</div>
						</div>

						<h3>Mis Géneros Favoritos</h3>
						<div class="genres-list">
							<span class="genre-tag">RPGs</span>
							<span class="genre-tag">Indie Games</span>
							<span class="genre-tag">Cyberpunk</span>
							<span class="genre-tag">Roguelikes</span>
							<span class="genre-tag">Narrativos</span>
							<span class="genre-tag">Sandbox</span>
						</div>

						<h3>Conecta Conmigo</h3>
						<p>
							¿Tienes alguna recomendación? ¿Quieres discutir sobre algún juego? 
							¡Me encanta conectar con otros gamers! Aunque este blog es principalmente 
							mi espacio personal para compartir mis experiencias gaming, siempre estoy 
							abierto a nuevas perspectivas y descubrimientos.
						</p>

						<div class="contact-note">
							<p>
								<strong>Nota:</strong> Todas las opiniones expresadas en este blog son personales 
								y basadas en mi experiencia como jugador. Mi objetivo es ayudar a otros gamers 
								a descubrir grandes juegos y evitar decepciones.
							</p>
						</div>
					</div>
				</div>
			</section>
		</div>
	</div>
</GlitchLayout>

<style>
	.about-container {
		padding: 2rem 0 4rem;
		min-height: calc(100vh - 80px);
	}

	.container {
		max-width: 1200px;
		margin: 0 auto;
		padding: 0 1rem;
	}

	/* Header de la página */
	.about-header {
		text-align: center;
		margin-bottom: 3rem;
	}

	.about-title {
		font-family: var(--font-orbitron);
		font-size: 2.5rem;
		font-weight: 600;
		color: var(--txt-main);
		margin: 0 0 0.5rem;
		text-transform: uppercase;
		letter-spacing: 0.05em;
	}

	.about-subtitle {
		color: var(--txt-muted);
		font-size: 1.1rem;
		margin: 0;
		max-width: 600px;
		margin-left: auto;
		margin-right: auto;
		line-height: 1.5;
	}

	/* Contenido principal */
	.about-content {
		max-width: 800px;
		margin: 0 auto;
	}

	.about-card {
		background: var(--bg-card);
		border-radius: 12px;
		padding: 2.5rem;
		box-shadow: var(--shadow);
		transition: transform 0.25s ease, box-shadow 0.25s ease;
	}

	.about-card:hover {
		transform: translateY(-2px);
		box-shadow: 0 0 16px rgba(0, 195, 255, 0.4);
	}

	/* Sección del avatar */
	.avatar-section {
		text-align: center;
		margin-bottom: 2.5rem;
		padding-bottom: 2rem;
		border-bottom: 2px solid rgba(0, 195, 255, 0.2);
	}

	.avatar {
		width: 180px;
		height: 180px;
		display: flex;
		align-items: center;
		justify-content: center;
		margin: 0 auto 1rem;
		transition: transform 0.3s ease;
	}

	.avatar:hover {
		transform: scale(1.05);
	}



	.gamer-tag {
		font-family: 'Orbitron', sans-serif;
		font-size: 1.8rem;
		color: var(--accent-2);
		margin: 0 0 0.5rem;
		text-transform: uppercase;
		letter-spacing: 0.05em;
	}

	.gamer-subtitle {
		color: var(--txt-muted);
		font-size: 1rem;
		margin: 0;
		font-weight: 500;
	}

	/* Sección de biografía */
	.bio-section h3 {
		font-family: 'Orbitron', sans-serif;
		color: var(--accent-2);
		font-size: 1.4rem;
		margin: 2rem 0 1rem;
		text-transform: uppercase;
		letter-spacing: 0.03em;
	}

	.bio-section p {
		color: var(--txt-main);
		line-height: 1.7;
		margin-bottom: 1.5rem;
		font-size: 1rem;
	}

	/* Grid de características */
	.features-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
		gap: 1.5rem;
		margin: 2rem 0;
	}

	.feature-item {
		background: var(--bg-main);
		padding: 1.5rem;
		border-radius: 8px;
		text-align: center;
		border: 2px solid transparent;
		transition: all 0.3s ease;
	}

	.feature-item:hover {
		border-color: var(--accent-2);
		transform: translateY(-3px);
		box-shadow: 0 5px 15px rgba(0, 195, 255, 0.2);
	}

	.feature-icon {
		font-size: 2rem;
		display: block;
		margin-bottom: 0.5rem;
	}

	.feature-item h4 {
		font-family: 'Orbitron', sans-serif;
		color: var(--txt-main);
		font-size: 1.1rem;
		margin: 0.5rem 0;
		text-transform: uppercase;
		letter-spacing: 0.02em;
	}

	.feature-item p {
		color: var(--txt-muted);
		font-size: 0.9rem;
		margin: 0;
		line-height: 1.5;
	}

	/* Lista de géneros */
	.genres-list {
		display: flex;
		flex-wrap: wrap;
		gap: 0.75rem;
		margin: 1.5rem 0;
	}

	.genre-tag {
		background: var(--bg-main);
		color: var(--accent-3);
		padding: 0.5rem 1rem;
		border-radius: 20px;
		font-size: 0.85rem;
		font-weight: 600;
		text-transform: uppercase;
		letter-spacing: 0.05em;
		border: 2px solid var(--accent-3);
		transition: all 0.25s ease;
	}

	.genre-tag:hover {
		background: var(--accent-3);
		color: var(--bg-main);
		transform: translateY(-2px);
		box-shadow: 0 4px 12px rgba(255, 213, 0, 0.3);
	}

	/* Nota de contacto */
	.contact-note {
		background: var(--bg-main);
		padding: 1.5rem;
		border-radius: 8px;
		border-left: 4px solid var(--accent-1);
		margin-top: 2rem;
	}

	.contact-note p {
		margin: 0;
		font-size: 0.9rem;
		color: var(--txt-muted);
		line-height: 1.6;
	}

	.contact-note strong {
		color: var(--accent-1);
	}

	/* Responsive design */
	@media (max-width: 768px) {
		.about-container {
			padding: 1rem 0 2rem;
		}

		.container {
			padding: 0 0.75rem;
		}

		.about-header {
			margin-bottom: 2rem;
		}

		.about-title {
			font-size: 2rem;
		}

		.about-subtitle {
			font-size: 1rem;
		}

		.about-card {
			padding: 1.5rem;
		}

		.avatar {
			width: 150px;
			height: 150px;
		}

		.avatar-emoji {
			font-size: 2.5rem;
		}

		.gamer-tag {
			font-size: 1.5rem;
		}

		.features-grid {
			grid-template-columns: 1fr;
			gap: 1rem;
		}

		.feature-item {
			padding: 1rem;
		}

		.genres-list {
			justify-content: center;
		}

		.bio-section h3 {
			font-size: 1.2rem;
		}
	}

	/* Animaciones de entrada */
	.about-card {
		animation: fadeInUp 0.6s ease forwards;
		opacity: 0;
		transform: translateY(20px);
	}

	@keyframes fadeInUp {
		to {
			opacity: 1;
			transform: translateY(0);
		}
	}

	/* Efectos adicionales */
	.feature-item:nth-child(1) { animation-delay: 0.1s; }
	.feature-item:nth-child(2) { animation-delay: 0.2s; }
	.feature-item:nth-child(3) { animation-delay: 0.3s; }

	.genre-tag:nth-child(odd) {
		animation: pulse 2s infinite;
	}

	@keyframes pulse {
		0%, 100% { opacity: 1; }
		50% { opacity: 0.7; }
	}
</style>